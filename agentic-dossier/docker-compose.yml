version: '3.8'

services:
  agentic-dossier:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app/backend
